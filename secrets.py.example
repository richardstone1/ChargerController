# Copy this file to `secrets.py` on your Raspberry Pi Pico (same folder as main.py).
# `secrets.py` is listed in .gitignore — do not commit real passwords to GitHub.

WIFI_SSID = "your-network-name"
WIFI_PASS = "your-wifi-password"
